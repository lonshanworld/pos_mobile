import "package:flutter/material.dart";

class CusVerticalDivider extends StatelessWidget {
  const CusVerticalDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const VerticalDivider(
      thickness: 0.5,
      color: Colors.grey,
    );
  }
}
